/**
 * Card Component Re-export
 * 
 * This file provides a convenient re-export of the RetroUI Card component
 * from the retroui subfolder. This allows components to import Card from
 * "@/components/Card" instead of "@/components/retroui/Card" for consistency
 * with other top-level component exports (Button, etc.).
 */
export { Card } from "@/components/retroui/Card";
